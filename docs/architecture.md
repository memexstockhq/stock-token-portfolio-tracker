# Architecture

stock-token-portfolio-tracker follows a layered design:

1. **Data layer** — chain RPCs, feeds, registries.
2. **Core layer** — stock token portfolio tracker logic.
3. **Interface layer** — CLI / API / UI.

See `examples/` for runnable flows.
